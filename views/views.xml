<odoo>
  
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cclog.list">
      <field name="name">cclog list</field>
      <field name="model">cclog.cclog</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cclog.action_window">
      <field name="name">cclog window</field>
      <field name="res_model">cclog.cclog</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cclog.action_server">
      <field name="name">cclog server</field>
      <field name="model_id" ref="model_cclog_cclog"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cclog" id="cclog.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cclog.menu_1" parent="cclog.menu_root"/>
    <menuitem name="Menu 2" id="cclog.menu_2" parent="cclog.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cclog.menu_1_list" parent="cclog.menu_1"
              action="cclog.action_window"/>
    <menuitem name="Server to list" id="cclog" parent="cclog.menu_2"
              action="cclog.action_server"/>
    -->
  
</odoo>